export enum TOPICS {
    SEND_EMAIL = 'send-email'
}

export enum API_ROUTE {
    USER = '/user',
    AUTH = '/auth',
    ACTIVITY = '/activity',
    TODO = '/todo'
}

export enum MESSAGE_RESPONSE {
    FAILED = 'failed',
    SUCCEED = 'succeed',
}
